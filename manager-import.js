(()=>{
'use strict';
const M=window.DBM;if(!M)return;
const wait=()=>{const top=document.querySelector('#materialsV2 .dbm-top');if(!top)return setTimeout(wait,100);init(top)};
function init(top){if(document.getElementById('dbmImportMaterials'))return;const btn=document.createElement('button');btn.id='dbmImportMaterials';btn.type='button';btn.className='secondary small';btn.textContent='Importa listino';const add=document.getElementById('dbmAddMaterial');if(add)add.insertAdjacentElement('beforebegin',btn);else top.appendChild(btn);const input=document.createElement('input');input.id='dbmImportMaterialsFile';input.type='file';input.accept='.json,application/json';input.hidden=true;document.body.appendChild(input);btn.onclick=()=>{input.value='';input.click()};input.onchange=()=>importFile(input.files?.[0])}
async function importFile(file){if(!file)return;let data;try{data=JSON.parse(await file.text())}catch{alert('File listino non valido.');return}const rows=Array.isArray(data)?data:Array.isArray(data?.materials)?data.materials:null;if(!rows){alert('Il file non contiene una lista materiali valida.');return}const existing=await M.mAll('materials'),key=x=>`${String(x.category||'').trim().toLowerCase()}|${String(x.name||'').trim().toLowerCase()}`;const seen=new Set(existing.map(key));let added=0,skipped=0,rejected=0;for(const r of rows){const name=String(r?.name||'').trim();if(!name){rejected++;continue}const category=M.categories.includes(r.category)?r.category:'Altro',k=key({name,category});if(seen.has(k)){skipped++;continue}const price=Math.max(0,M.n(r.lastPrice??r.price)),supplier=String(r.supplier||'').trim(),lastDate=String(r.lastDate||r.date||'').trim(),notes=String(r.notes||'').trim();await M.mPut('materials',{id:M.uid(),name,category,supplier,lastPrice:price,lastDate,notes});if(price>0&&lastDate)await M.mPut('history',{id:M.uid(),materialId:(await M.mAll('materials')).find(x=>key(x)===k)?.id||'',date:lastDate,price,supplier,source:'catalog-import'});seen.add(k);added++}await M.renderMaterials();alert(`${added} materiali importati.${skipped?` ${skipped} già presenti, non modificati.`:''}${rejected?` ${rejected} righe non valide ignorate.`:''}`)}
wait();
})();
